import { AppRedDirective } from './app-red.directive';

describe('AppRedDirective', () => {
  it('should create an instance', () => {
    const directive = new AppRedDirective();
    expect(directive).toBeTruthy();
  });
});
